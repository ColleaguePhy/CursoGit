Mi primera línea

:wq = guarda y sale
:q = sale sin guardar
:!q = fuerza a salir

Segunda línea xd jaja lol (se hizo para hacer el primer commit)

Esta linea se agregó para hacer el segundo commit, ademas se agregaron otros
comandos.

mkdir = crear carpeta en consola
vim = crear archivos si no existen y si existe acceder a ellos
git add = sube cambios al area de preparación
git commit = sube los cambios al repositorio

esta linea se agregó para mostrar las diferencias entre las diferentes
versiones, ademas se agregaron nuevos comandos.

git log = muestra las versiones en el repositorio (por lo menos por ahora)

git diff = muestra las diferencias entre versiones o por su defecto entre el
area de trabajo. Se puede agregar el comando HEAD + nombre para a la ultima
versión. HEAD + ~1 compara con la versión inmediatamente anterior.
